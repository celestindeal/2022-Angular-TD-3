import { ClasseBagageDirective } from './classe-bagage.directive';

describe('ClasseBagageDirective', () => {
  it('should create an instance', () => {
    const directive = new ClasseBagageDirective();
    expect(directive).toBeTruthy();
  });
});
